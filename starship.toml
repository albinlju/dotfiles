"$schema" = 'https://starship.rs/config-schema.json'

# Inserts a blank line between shell prompts
add_newline = true

format = '''
[┌──>](bold purple)$git_branch$git_status$fill$dotnet$lua$nodejs
[│](bold purple)$nix_shell$directory
[└>](bold green) '''

[nix_shell]
format = '[ $symbol]($style)'
